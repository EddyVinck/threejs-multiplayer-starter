- [x] P0: Initialize the pnpm workspace with `client`, `server`, and `shared` packages, root scripts, strict TypeScript config, ESLint, Prettier, Vitest, and a single `pnpm dev` entrypoint orchestrated with `concurrently`.
- [x] P0: Establish the shared contract kit first, including message envelopes, protocol event names, Zod schemas, DTOs, room code utilities, and pure serialization helpers.
- [x] P0: Define the core simulation model in `shared` and `server` terms before building rendering, including entity state shape, pickup rules, score state, round timer state, and reset semantics.
- [x] P0: Build the simulation core as a deep module with a small public interface for fixed-step updates, snapshot export, delta production, round reset, and late-join hydration.
- [x] P0: Write the first automated tests for the shared contract kit, covering schema validation, room code generation, message envelope behavior, and pure utility functions.
- [x] P0: Write the first automated tests for the simulation core, covering pickup collection, score updates, timer progression, round reset behavior, and deterministic update outcomes.

- [x] P0: Build the server foundation with Express, Socket.IO, and a single global authoritative tick loop.
- [x] P0: Implement the room runtime as a deep server module that owns room creation, quick join matching, room-code joins, public ephemeral room policy, membership, late join eligibility, and room lifecycle.
- [x] P0: Add automated tests for the room runtime that validate room creation, quick join behavior, join-by-code behavior, late join, and round lifecycle transitions through the public API.
- [x] P0: Implement transport boundary validation with Zod for all incoming socket payloads and outgoing snapshot or delta contracts.
- [x] P0: Implement the realtime transport layer as a deep module that hides Socket.IO wiring behind stable interfaces for connect, disconnect, join, snapshot delivery, delta delivery, and command submission.

- [x] P0: Build the local loopback session adapter so single-player and multiplayer share the same session interface from the beginning.
- [x] P0: Build the session orchestrator as a deep module that selects single-player, quick join, create room, or join by code, and hides loopback versus remote transport details from the rest of the app.
- [x] P0: Ensure URL-based room join support is represented in the session entry flow from the start, even before the UI is polished.

- [x] P1: Scaffold the Vite client with a minimal boot flow, canvas mounting, DOM overlay root, and asset-light startup path.
- [x] P1: Implement the persistence layer as a deep module for player name, volume, mute, and lightweight settings with validation, versioning, and fallback behavior.
- [x] P1: Add automated tests for the persistence layer covering save, load, migration, corruption fallback, and default restoration behavior.
- [x] P1: Implement the player command pipeline as a deep client module that captures keyboard and mouse input, normalizes it into high-level commands, and emits compact command snapshots at a fixed network rate.
- [x] P1: Add automated tests for the player command pipeline covering device normalization and command snapshot generation behavior.

- [x] P1: Integrate Rapier 3D through a physics adapter that hides engine setup, body creation, colliders, stepping, and gameplay-facing queries behind a simple interface.
- [x] P1: Implement free-flying kinematic or player-driven movement against a mostly static world using the physics adapter.
- [x] P1: Build the third-person orbit or chase camera controller as a deep module with smoothing, target follow behavior, and mouse-driven orbit input.
- [x] P1: Build the render scene adapter to map simulation state into Three.js scene objects, minimal materials, simple lighting, and debug-friendly primitives.
- [x] P1: Keep the initial rendering path intentionally lightweight and avoid heavy starter assets, post-processing, or large downloads.

- [x] P1: Build the tiny sample arena or playground with minimal primitives and clear navigable space.
- [ ] P1: Implement the arena gameplay module with pickup spawning, pickup collection, per-player score tracking, match timer, and round reset flow.
- [ ] P1: Make sure the sample mode works both through the loopback single-player path and through the multiplayer room path without special-case gameplay logic.
- [ ] P1: Verify late join by sending an authoritative room snapshot followed by live updates and ensuring a new client can enter an in-progress round in a valid state.

- [ ] P1: Build the lightweight DOM/CSS UI shell with a small pre-game screen, single-player as the primary CTA, and multiplayer actions visible but secondary.
- [ ] P1: Add create room, quick join, and join-by-code flows to the UI shell, including room-link handling via URL parameters.
- [ ] P1: Add local player name editing and settings controls to the UI shell with persistence integration.
- [ ] P1: Add a minimal in-game HUD showing score, round timer, and room state relevant to the current mode.

- [ ] P2: Build the Web Audio API-based audio manager as a deep module with unlock-on-user-gesture behavior, master gain, mute, volume, and simple named playback hooks.
- [ ] P2: Integrate lightweight audio cues for pickup collection, UI interactions, and round transitions without introducing large bundled audio assets.
- [ ] P2: Add client-side diagnostics for FPS, connection state, and other lightweight runtime signals behind a debug toggle.
- [ ] P2: Add manual smoke-test scripts or checklists for single-player start, room creation, quick join, join by code, URL join, late join, round reset, reconnect behavior, and production build startup.
- [ ] P2: Add production build scripts so the client builds separately and is served statically by the server in the final deployable app shape.
- [ ] P2: Validate the final package against the jam constraints by checking startup time, avoiding loading screens, minimizing first-load weight, and confirming no login or signup flows exist.
- [ ] P2: Review all deep module interfaces after the first end-to-end pass and simplify any surface area that became too chatty or leaked implementation details.

## Progress Log

- 2026-04-03: Completed the workspace bootstrap P0 task by adding the root pnpm workspace, strict TypeScript base config, ESLint/Prettier/Vitest tooling, `pnpm dev` orchestration with `concurrently`, and minimal `client`, `server`, and `shared` package skeletons. Files changed: `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `tsconfig.base.json`, `tsconfig.json`, `eslint.config.mjs`, `.prettierrc.json`, `.prettierignore`, `.gitignore`, `packages/client/*`, `packages/server/*`, `packages/shared/*`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm lint` passed, `pnpm typecheck` passed, `pnpm test` passed with no test files, and `pnpm build` passed. Next recommended task: establish the shared contract kit with protocol envelopes, Zod schemas, DTOs, room-code helpers, and pure serialization utilities.
- 2026-04-03: Completed the shared contract kit P0 task by adding versioned protocol envelopes, shared event names, Zod-validated DTO and snapshot schemas, room-code generation and normalization helpers, JSON serialization utilities, and focused Vitest coverage for the public contract surface. Files changed: `pnpm-lock.yaml`, `packages/shared/package.json`, `packages/shared/src/index.ts`, `packages/shared/src/index.test.ts`, `packages/shared/src/protocol.ts`, `packages/shared/src/room-code.ts`, `packages/shared/src/schemas.ts`, `packages/shared/src/serialization.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm lint` passed, `pnpm typecheck` passed, `pnpm test` passed, and `pnpm build` passed. Next recommended task: define the core simulation model in shared and server terms, including entity state shape, pickup rules, score state, round timer state, and reset semantics.
- 2026-04-04: Completed the simulation model P0 task by adding a dedicated shared simulation module with authoritative room, player, pickup, arena, scoring, and round/reset state schemas plus default rules and focused coverage for the new public model surface. Files changed: `packages/shared/src/index.ts`, `packages/shared/src/index.test.ts`, `packages/shared/src/simulation.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: build the simulation core as a deep module with a small public interface for fixed-step updates, snapshot export, delta production, round reset, and late-join hydration.
- 2026-04-04: Completed the simulation core P0 task by adding a deep authoritative runtime module for player registration and command intake, fixed-step movement and pickup resolution, snapshot and delta export, timer-driven and manual round reset handling, and late-join hydration, plus initial Vitest coverage for joins, scoring, resets, and deterministic replay behavior. Files changed: `pnpm-lock.yaml`, `packages/server/package.json`, `packages/server/tsconfig.json`, `packages/server/src/simulation-core.ts`, `packages/server/src/simulation-core.test.ts`, `specs/boilerplate-implementation-plan.md`, and `vitest.config.ts`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: build the server foundation with Express, Socket.IO, and a single global authoritative tick loop.
- 2026-04-04: Completed the server foundation P0 task by introducing a reusable Express plus Socket.IO server module, a global authoritative tick loop that can register room runtimes behind a small interface, a production entrypoint that boots and shuts down the foundation cleanly, and focused Vitest coverage for both the tick loop and `/health` startup surface. Files changed: `pnpm-lock.yaml`, `packages/server/package.json`, `packages/server/src/index.ts`, `packages/server/src/server-foundation.ts`, `packages/server/src/server-foundation.test.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: implement the room runtime as a deep server module that owns room creation, quick join matching, room-code joins, public ephemeral room policy, membership, late join eligibility, and room lifecycle.
- 2026-04-04: Completed the room runtime P0 task by adding a deep multiplayer room registry and per-room runtime that wrap the simulation core for room creation, quick join matching, join-by-code, membership and disconnect handling, public room lifecycle cleanup, and late-join admission rules, while extending deltas to represent player removals and adding focused runtime coverage for the new public API. Files changed: `packages/shared/src/schemas.ts`, `packages/server/src/simulation-core.ts`, `packages/server/src/room-runtime.ts`, `packages/server/src/room-runtime.test.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: add dedicated automated tests for the room runtime that validate room creation, quick join behavior, join-by-code behavior, late join, and round lifecycle transitions through the public API.
- 2026-04-04: Completed the room runtime coverage P0 task by expanding the dedicated Vitest suite to assert private room creation defaults, registry lookup behavior, quick-join room selection, normalized join-by-code behavior, late-join rejection, round lifecycle transitions through the room runtime public API, and empty-room cleanup semantics. Files changed: `packages/server/src/room-runtime.test.ts` and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: implement transport boundary validation with Zod for all incoming socket payloads and outgoing snapshot or delta contracts.
- 2026-04-04: Completed the transport boundary validation P0 task by adding a focused server transport validation module that parses each incoming client envelope against its event-specific Zod schema, validates outgoing server envelopes before emission, normalizes envelope payloads through shared contracts, and adds Vitest coverage for accepted and rejected transport boundary cases. Files changed: `packages/server/package.json`, `packages/server/src/transport-boundary.ts`, `packages/server/src/transport-boundary.test.ts`, `pnpm-lock.yaml`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: implement the realtime transport layer as a deep module that hides Socket.IO wiring behind stable interfaces for connect, disconnect, join, snapshot delivery, delta delivery, and command submission.
- 2026-04-04: Completed the realtime transport P0 task by adding a dedicated Socket.IO transport module that owns connection lifecycle, validated join and command handlers, session tracking, room snapshot delivery, room delta fanout from authoritative ticks, and protocol error reporting, while keeping the server foundation generic and adding end-to-end socket-level coverage for quick join, create room, join by code, command submission, delta delivery, and disconnect cleanup. Files changed: `package.json`, `pnpm-lock.yaml`, `packages/server/src/index.ts`, `packages/server/src/realtime-transport.ts`, `packages/server/src/realtime-transport.test.ts`, `packages/server/src/room-runtime.ts`, `packages/server/src/server-foundation.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: build the local loopback session adapter so single-player and multiplayer share the same session interface from the beginning.
- 2026-04-04: Completed the loopback session adapter P0 task by extracting the pure simulation core into `shared`, defining a client-facing session interface that hides transport-specific details, and adding a local single-player loopback adapter plus focused Vitest coverage for joined-state replay, authoritative delta delivery, and stop semantics. Files changed: `pnpm-lock.yaml`, `packages/client/package.json`, `packages/client/src/loopback-session.test.ts`, `packages/client/src/loopback-session.ts`, `packages/client/src/session.ts`, `packages/server/src/simulation-core.ts`, `packages/shared/src/index.ts`, `packages/shared/src/simulation-core.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm build` passed, `pnpm test` passed, `pnpm typecheck` passed, and `pnpm lint` passed. Next recommended task: build the session orchestrator as a deep module that selects single-player, quick join, create room, or join by code, and hides loopback versus remote transport details from the rest of the app.
- 2026-04-04: Completed the session orchestrator P0 task by adding a deep client-side orchestration layer that starts single-player loopback sessions or remote multiplayer sessions behind one stable API, introducing a Socket.IO-backed remote session client with validated join and delta handling, sharing snapshot-application helpers across session adapters, wiring the client entrypoint through the orchestrator, and adding focused Vitest coverage for remote session startup plus orchestration and stale-start abort behavior. Files changed: `pnpm-lock.yaml`, `packages/client/package.json`, `packages/client/src/loopback-session.ts`, `packages/client/src/main.ts`, `packages/client/src/remote-session.test.ts`, `packages/client/src/remote-session.ts`, `packages/client/src/session-orchestrator.test.ts`, `packages/client/src/session-orchestrator.ts`, `packages/client/src/session-snapshot.ts`, `packages/client/src/session.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: ensure URL-based room join support is represented in the session entry flow from the start, even before the UI is polished.
- 2026-04-04: Completed the URL-based room join P0 task by adding a small client session-entry resolver that inspects invite-link query parameters, normalizes valid room codes into the existing join-by-code session flow, falls back safely to single-player for invalid links, and updates the minimal boot path to surface room-link startup intent before the polished UI shell exists. Files changed: `packages/client/src/main.ts`, `packages/client/src/session-entry.test.ts`, `packages/client/src/session-entry.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, and `pnpm lint` passed. Next recommended task: scaffold the Vite client with a minimal boot flow, canvas mounting, DOM overlay root, and asset-light startup path.
- 2026-04-04: Completed the Vite client boot scaffold P1 task by replacing the temporary text-only startup screen with a mounted canvas and overlay shell, adding a lightweight painted boot backdrop plus status view models for single-player and room-link startup states, wiring the session orchestrator updates through that scaffold, and adding focused Vitest coverage for the new boot status mapping behavior. Files changed: `packages/client/src/boot-shell.ts`, `packages/client/src/boot-status.test.ts`, `packages/client/src/boot-status.ts`, `packages/client/src/main.ts`, `packages/client/src/styles.css`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: implement the persistence layer as a deep module for player name, volume, mute, and lightweight settings with validation, versioning, and fallback behavior.
- 2026-04-04: Completed the persistence layer P1 task by adding a dedicated client settings store for display name and audio preferences with storage-safe reads and writes, explicit schema-versioned persistence, legacy-shape migration, corruption fallback back to normalized defaults, and boot-time hydration so persisted display names flow into session startup without transport-specific code. Files changed: `packages/client/src/main.ts`, `packages/client/src/persistence.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: add automated tests for the persistence layer covering save, load, migration, corruption fallback, and default restoration behavior.
- 2026-04-04: Completed the persistence-layer test coverage P1 task by adding focused Vitest coverage for versioned save and reload behavior, legacy-key migration into the current record, corruption fallback back to normalized defaults, invalid-field restoration to configured defaults during load, and reset-driven default restoration persistence. Files changed: `packages/client/src/persistence.test.ts` and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm exec vitest run packages/client/src/persistence.test.ts` passed, `pnpm typecheck` passed, and `pnpm lint` passed. Next recommended task: implement the player command pipeline as a deep client module that captures keyboard and mouse input, normalizes it into high-level commands, and emits compact command snapshots at a fixed network rate.
- 2026-04-04: Completed the player command pipeline P1 task by adding a dedicated client input module that owns keyboard and mouse capture on the game canvas, normalizes those inputs into shared `PlayerCommand` snapshots with yaw, pitch, movement, and action state, samples them at a fixed rate aligned with the simulation tick, and wires that pipeline into session startup so both loopback and remote sessions automatically receive live command updates. Files changed: `packages/client/src/main.ts`, `packages/client/src/player-command-pipeline.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: add automated tests for the player command pipeline covering device normalization and command snapshot generation behavior.
- 2026-04-04: Completed the player command pipeline test coverage P1 task by adding focused Vitest coverage for keyboard and mouse normalization, sampled command payload generation, blur-driven transient input clearing, and stop behavior so the client input contract stays stable before physics and rendering integration. Files changed: `packages/client/src/player-command-pipeline.test.ts` and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm exec vitest run packages/client/src/player-command-pipeline.test.ts` passed, `pnpm typecheck` passed, and `pnpm lint` passed. Next recommended task: integrate Rapier 3D through a physics adapter that hides engine setup, body creation, colliders, stepping, and gameplay-facing queries behind a simple interface.
- 2026-04-04: Completed the Rapier physics adapter P1 task by adding a dedicated client physics module backed by `@dimforge/rapier3d` that owns world and arena setup, kinematic player bodies, sensor pickup colliders, fixed-step stepping, gameplay-facing movement resolution against arena and player blockers, and pickup overlap queries, plus focused Vitest coverage for overlap and motion behavior and a production build validation that the Vite client bundles the Rapier WASM path successfully. Files changed: `packages/client/package.json`, `packages/client/src/physics-adapter.ts`, `packages/client/src/physics-adapter.test.ts`, `pnpm-lock.yaml`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm exec vitest run packages/client/src/physics-adapter.test.ts` passed, `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: implement free-flying kinematic or player-driven movement against a mostly static world using the physics adapter.
- 2026-04-04: Completed the physics-backed movement P1 task by adding a dedicated client movement runtime that consumes authoritative room snapshots, rebuilds the local physics world from shared room config, advances the local player through free-flying kinematic motion against arena and player blockers, and keeps the movement math aligned with the shared simulation helpers, while extending room snapshots to carry arena and rules metadata and adding focused coverage for local motion, blockers, and snapshot contract updates. Files changed: `packages/client/src/boot-status.test.ts`, `packages/client/src/movement-runtime.test.ts`, `packages/client/src/movement-runtime.ts`, `packages/client/src/remote-session.test.ts`, `packages/client/src/session-snapshot.ts`, `packages/server/src/transport-boundary.test.ts`, `packages/shared/src/index.test.ts`, `packages/shared/src/schemas.ts`, `packages/shared/src/simulation-core.ts`, `packages/shared/src/simulation.ts`, `packages/shared/dist/*`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm test` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: build the third-person orbit or chase camera controller as a deep module with smoothing, target follow behavior, and mouse-driven orbit input.
- 2026-04-04: Completed the camera-controller P1 task by adding a dedicated client camera module that tracks the authoritative local player target, converts normalized look input into third-person orbit or chase poses, smooths both camera position and follow target movement, and adds focused Vitest coverage for chase placement, orbit input, pitch clamping, smoothing, and missing-player handling. Files changed: `packages/client/src/camera-controller.ts`, `packages/client/src/camera-controller.test.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm exec vitest run packages/client/src/camera-controller.test.ts packages/client/src/movement-runtime.test.ts packages/client/src/player-command-pipeline.test.ts` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm build` passed. Next recommended task: build the render scene adapter to map simulation state into Three.js scene objects, minimal materials, simple lighting, and debug-friendly primitives.
- 2026-04-04: Completed the render scene adapter P1 task by adding a lightweight Three.js scene runtime that maps authoritative room snapshots into arena, player, and pickup primitives with simple lighting, reuses the movement runtime and camera controller for local prediction and third-person framing, wires the adapter into the client boot flow so session updates drive the mounted canvas, and adds focused coverage for snapshot-driven scene sync plus predicted local motion. Files changed: `package.json`, `pnpm-lock.yaml`, `packages/client/package.json`, `packages/client/src/main.ts`, `packages/client/src/render-scene-adapter.test.ts`, `packages/client/src/render-scene-adapter.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm exec vitest run packages/client/src/render-scene-adapter.test.ts` passed, `pnpm typecheck` passed, and `pnpm lint` passed. Next recommended task: keep the initial rendering path intentionally lightweight and avoid heavy starter assets, post-processing, or large downloads.
- 2026-04-04: Completed the lightweight-rendering P1 task by adding a small client render-budget helper that caps device-pixel-ratio and total framebuffer pixels for the boot canvas, explicitly disabling heavier renderer features such as shadows and tone mapping, confirming the starter still ships with no bundled art or audio assets so the initial visual path stays primitive-first and download-light, and fixing the existing client build verification gap by marking `.wasm` files as Vite client assets for the Rapier runtime. Files changed: `packages/client/src/boot-shell.ts`, `packages/client/src/render-budget.ts`, `packages/client/src/render-budget.test.ts`, `packages/client/src/render-scene-adapter.ts`, `packages/client/vite.config.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm exec vitest run packages/client/src/render-budget.test.ts packages/client/src/render-scene-adapter.test.ts` passed, `pnpm typecheck` passed, `pnpm lint` passed, and `pnpm --filter @gamejam/client build` passed with a large chunk size warning due to the bundled Rapier payload. Next recommended task: build the tiny sample arena or playground with minimal primitives and clear navigable space.
- 2026-04-04: Completed the sample-arena P1 task by adding a shared canonical playground layout with multi-lane spawn and pickup placement plus simple box structures, extending the shared arena contract so snapshots can carry navigable structure geometry, reusing that same sample arena for loopback and multiplayer room creation, and teaching the client physics and render adapters to block and draw those structures with focused coverage for the new arena path. Files changed: `packages/shared/src/index.ts`, `packages/shared/src/index.test.ts`, `packages/shared/src/sample-arena.ts`, `packages/shared/src/schemas.ts`, `packages/shared/src/simulation.ts`, `packages/shared/dist/*`, `packages/client/src/loopback-session.ts`, `packages/client/src/physics-adapter.ts`, `packages/client/src/physics-adapter.test.ts`, `packages/client/src/render-scene-adapter.ts`, `packages/client/src/render-scene-adapter.test.ts`, `packages/client/src/camera-controller.test.ts`, `packages/client/src/boot-status.test.ts`, `packages/client/src/movement-runtime.test.ts`, `packages/client/src/remote-session.test.ts`, `packages/server/src/room-runtime.ts`, `packages/server/src/simulation-core.test.ts`, `packages/server/src/transport-boundary.test.ts`, and `specs/boilerplate-implementation-plan.md`. Checks run: `pnpm lint` passed, `pnpm typecheck` passed, `pnpm exec vitest run packages/shared/src/index.test.ts packages/client/src/physics-adapter.test.ts packages/client/src/render-scene-adapter.test.ts packages/client/src/movement-runtime.test.ts packages/server/src/simulation-core.test.ts packages/server/src/transport-boundary.test.ts packages/client/src/remote-session.test.ts packages/client/src/camera-controller.test.ts packages/client/src/boot-status.test.ts` passed, and `pnpm build` passed with the existing Vite client chunk-size warning for the Rapier payload. Next recommended task: implement the arena gameplay module with pickup spawning, pickup collection, per-player score tracking, match timer, and round reset flow.
